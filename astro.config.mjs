// @ts-check
import { defineConfig } from 'astro/config';

// Minimal static site. White-on-black, lowercase, Helvetica Neue — matches the deck.
export default defineConfig({
  site: 'https://elisatoworld.vercel.app',
});
